import hasAuth from './hasAuth';
import {
  setSession,
  setLocal,
  getLocal,
  getSession,
  clear,
  remove
} from './storage';
import { goPageG } from './goPage';

export {
  hasAuth,
  setSession,
  setLocal,
  getLocal,
  getSession,
  clear,
  remove,
  goPageG
};
