import hasAuth from './hasAuth';
import {
  setSession,
  setLocal,
  getLocal,
  getSession,
  clear,
  remove
} from './storage';

export default {
  hasAuth,
  setSession,
  setLocal,
  getLocal,
  getSession,
  clear,
  remove
};
